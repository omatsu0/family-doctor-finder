%h5= @area.name

.table-responsive.pt-5
  %table.table.table-sm
    %thead.bg-info.text-white
      %tr
        %th 地区ID
        %th 病院名
        %th よみがな
        %th 診療科
        %th 
    %tbody
      - @locations.each do |area_clinic|
        %tr{"data-link" => "#{clinic_path(area_clinic.clinic.id)}"}
          %td= area_clinic.clinic.location.area.name
          %td= area_clinic.clinic.clinic_name
          %td= area_clinic.clinic.clinic_furigana
          %td= area_clinic.clinic.departments.pluck(:department_name)
          %td= area_clinic.clinic.location.area.name

:javascript
  $("tr[data-link]").click(function() {
    window.location = $(this).data("link")
  })

-# - @locations.each do |area_clinic|
-#   = link_to(area_clinic.clinic, class: "list-group-item list-group-item-action") do
-#     = area_clinic.clinic.clinic_name

-# = debug @locations.clinic
-# %h5 ああああ
-# = debug @area.locations