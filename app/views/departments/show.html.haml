%h5= @department.department_name

- @department.clinic_departments.each do |clinic_department|
  = link_to(clinic_department.clinic, class: "list-group-item list-group-item-action") do
    = clinic_department.clinic.clinic_name
  
  = debug clinic_department.clinic.id